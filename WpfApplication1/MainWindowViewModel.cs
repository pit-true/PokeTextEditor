namespace MenuExt
{
    public class MainWindowViewModel
    {
       // public ReactiveCommand s1Command { get; } = new ReactiveCommand();
       // public ReactiveProperty<string> MyText { get; } = new ReactiveProperty<string>();

        public MainWindowViewModel()
        {
          //  s1Command.Subscrive(_ => Text.Value = Replace.shortcutMoji(1);
            
        }
    }
}